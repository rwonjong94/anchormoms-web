# ==============================================
# Frontend Environment Variables
# ==============================================
# Copy this file to .env.local and fill in your values

# -----------------
# Public Variables (Exposed to Browser)
# -----------------
# These are accessible in the browser via process.env.NEXT_PUBLIC_*

# Backend API URL (as seen from the browser)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Base URL for images
NEXT_PUBLIC_IMAGE_BASE_URL=http://localhost:3000

# Kakao OAuth Client ID
NEXT_PUBLIC_KAKAO_CLIENT_ID=your-kakao-client-id

# Google OAuth Client ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Redirect URI
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google

# -----------------
# Server-Side Only Variables
# -----------------
# These are ONLY available in API routes and server components

# Backend API URL (internal, server-to-server)
# In development: http://localhost:3001
# In Docker: http://backend:3001
BACKEND_URL=http://localhost:3001

# OAuth Secrets (Server-side only)
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------
# NextAuth (if used)
# -----------------
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key

# -----------------
# AI APIs (Server-side - for OCR feature)
# -----------------
# ⚠️ Note: These should ideally be moved to Backend
# Currently used in /api/qbank/ocr route
GEMINI_API_KEY=your-gemini-api-key
GOOGLE_CLOUD_API_KEY=your-google-cloud-vision-api-key

# -----------------
# Books Directory (for QBank)
# -----------------
# Path to books directory for problem bank
BOOKS_BASE_DIR=/var/www/books

# -----------------
# Application Settings
# -----------------
NODE_ENV=development
PORT=3000

# -----------------
# ⚠️ DO NOT INCLUDE THESE IN FRONTEND
# -----------------
# The following should NEVER be in frontend:
# ❌ DATABASE_URL - Backend only
# ❌ JWT_SECRET - Backend only
# ❌ ADMIN_PASSWORD - Backend only
# ❌ REDIS credentials - Backend only
# ❌ RTZR credentials - Backend only
